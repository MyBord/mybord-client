import * as React from 'react';
import UserCardMenuContentContainer, { Props } from './userCardMenuContentContainer';

const CardMenuContent: React.FC<Props> = (props) => (
  <UserCardMenuContentContainer {...props} />
);

export default CardMenuContent;
