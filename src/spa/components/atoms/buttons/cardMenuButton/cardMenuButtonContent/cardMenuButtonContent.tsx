import * as React from 'react';
import CardMenuButtonContentContainer, { Props } from './cardMenuButtonContentContainer';

const CardMenuButtonContent: React.FC<Props> = (props) => (
  <CardMenuButtonContentContainer {...props} />
);

export default CardMenuButtonContent;
