import * as React from 'react';
import LoginFormContentContainer from './loginFormContentContainer';

const LoginFormContent: React.FC = () => <LoginFormContentContainer />;

export default LoginFormContent;
