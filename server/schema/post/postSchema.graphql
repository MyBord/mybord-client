# ----- INPUTS ----- #

input CreatePostInput {
  author: ID!,
  body: String!,
  published: Boolean!,
  title: String!,
}

# ----- TYPES ----- #

type Post {
  id: ID!,
  author: User!,
  body: String!,
  comments: [Comment!]!,
  published: Boolean!,
  title: String!,
}
